```java
public boolean equals(object obj){
    if (obj instanceof Person){ // 检查是否是同一个类  但是一般用getclass()
        boolean s = super.equals(obj);
        Person p = (Person) obj;
        return s && 

    }
}
```
